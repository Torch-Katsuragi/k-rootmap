# FEATURES.md

<!--
K-MAPS 機能設計・仕様まとめ
このファイルはREADME.mdから分離したfeature設計ドキュメントです。
-->

# K-MAPS構想（Fresco風UI）

## 1. コンセプト

「お絵描きアプリのような直感的な操作性で、誰でも気軽にGeoPackageデータを編集・作成できるモバイルアプリ K-MAPS」

### ターゲットユーザー
- フィールド調査を行う研究者・技術者
- GISの専門知識はないが、位置情報付きのメモやスケッチを作成したい一般ユーザー
- QFieldのような高機能GISアプリよりも、手軽さを求めるユーザー

### コアバリュー
- **直感的なUI/UX**: Adobe Frescoのようなツール切り替えやレイヤー操作で、学習コストを低くする。
- **手軽さ**: 専門知識がなくても、地図上にお絵描きする感覚で地理空間データを作成・編集できる。
- **モバイルファースト**: スマートフォンやタブレットでのGPS連携やタッチ操作に最適化する。
- **Googleエコシステム連携**: Google Driveへの保存・同期をスムーズに行う。

---

## 2. 主要機能案

### 2.1. 描画・編集機能
- **基本図形描画**
    - 点 (Point): タップで配置。アイコンや色のカスタマイズ。
    - 線 (LineString): フリーハンド描画、タップによる頂点指定。太さ、色、線種の変更。
    - 領域 (Polygon): フリーハンド描画、タップによる頂点指定。塗りつぶし色、透過度、境界線の設定。
- **属性情報編集**
    - 描画した地物（フィーチャ）ごとに、テキスト、数値、写真などの属性情報を簡単に入力・編集。
    - 事前に定義した属性スキーマ、または自由形式での入力。
- **編集ツール**
    - 選択ツール
    - 移動ツール
    - 頂点編集ツール
    - 削除ツール
    - 元に戻す/やり直し (Undo/Redo)
- **計測ツール**
    - 距離計測
    - 面積計測
- **地図操作ツール（MapTool）を導入し、「てのひら」「ペン」「選択」などのツールを切り替え可能。**
- **ペン入力（スタイラス）とタップ入力（指・マウス）で挙動を分岐し、ペンはフリーハンド描画、タップは点追加や選択など直感的な操作性を実現。**

### 2.2. レイヤー管理機能
- Adobe Frescoのような直感的なレイヤーパネル。
- フォルダ構造とGeoPackageファイルに連動したレイヤー/レイヤーグループ:
    - プロジェクトフォルダ内のサブフォルダ構造が、K-MAPS内のレイヤーグループ階層としてそのまま反映される。
    - 各フォルダ内に存在するGeoPackageファイル（.gpkg）は、そのフォルダに対応するレイヤーグループ内に「GeoPackageソース」のような特別なアイテムとして表示される。
    - 「GeoPackageソース」アイテムを展開すると、そのGeoPackageファイル内に保存されている各レイヤーが一覧表示される。
    - これにより、「フォルダグループ → （サブフォルダグループ / GeoPackageソース → GeoPackage内部レイヤー）」という階層構造を実現する。
- レイヤー/レイヤーグループ/GeoPackageソースの追加、削除、複製、名称変更。
    - フォルダやGeoPackageファイルの操作は、実際のファイルシステム構造にも反映される（またはその逆）。
- レイヤーの表示/非表示切り替え（個々のレイヤー、GeoPackageソース全体、フォルダグループ全体）。
- レイヤー/レイヤーグループ/GeoPackageソースの順序変更（ドラッグ＆ドロップ）。
- レイヤーごとの透過度設定。

### 2.3. GPS・地図機能
- **GPSトラッキング**
    - 現在位置表示。
    - 移動軌跡の記録（ラインフィーチャとして）。
    - GPS精度、速度などの情報表示。
- **背景地図**
    - `flutter_map` を利用 (OpenStreetMapなど各種タイルソースに対応)
    - オフラインタイルマップ (MBTilesなど、flutter_mapの機能で対応)
- **地図操作**
    - パン、ズーム (ピンチイン/アウト)
    - 回転
    - 現在位置へ移動

### 2.4. プロジェクトとGeoPackage管理
- **プロジェクトの概念**
    - K-MAPSでの作業単位は「プロジェクト」とし、各プロジェクトは専用のフォルダでローカルストレージに保存・管理されることを基本とする。
    - プロジェクトフォルダ内およびそのサブフォルダ内に、複数のGeoPackageファイル（.gpkg）を格納できる。
    - K-MAPSはプロジェクトフォルダ内を再帰的にスキャンし、全てのGeoPackageファイルを認識する。
    - プロジェクトフォルダのルートには、プロジェクト設定やメタデータ（例: `project_meta.json`）を保存する。
    - オプションとして、このメタデータ内にGoogle Driveの特定のフォルダへのアクセス情報（フォルダIDなど）を保持し、ユーザーの任意のタイミングでの同期を可能にする。
    - Gitによるバージョン管理も視野に入れる。
- **新規プロジェクト作成**
    - ローカルで作成する場合: プロジェクト名（フォルダ名として使用）とローカルの保存場所を指定。プロジェクトフォルダを自動生成。
    - Google Driveから作成する場合: 後述の「Google Drive連携」セクションの通り、指定されたDriveフォルダをローカルに複製し、連携情報をメタデータに保存する。
    - 初期GeoPackageファイルの作成はオプションとするか、最初のレイヤー作成時に促す。
- **既存プロジェクトを開く**
    - ローカルストレージからプロジェクトフォルダを選択して開く。
    - Google Driveから既存プロジェクトを開く場合も、一度ローカルにプロジェクトを複製（または最新状態に同期）してから開くことを基本とする。
    - フォルダ内の全てのGeoPackageファイルを再帰的に読み込み、フォルダ構造とGeoPackageファイルの配置をレイヤーパネルに正確に反映する。
- **GeoPackage保存**
    - 編集内容は、対応するGeoPackageファイルに保存される。
- **属性テーブル表示・簡易編集** (K-MAPS内での確認用)

### 2.5. Google Drive連携
- Googleアカウントでのログイン。
- プロジェクトとGoogle Driveフォルダの同期:
    - プロジェクトは基本的にローカルに保存される。
    - プロジェクトメタデータにGoogle Driveのフォルダアクセス情報が保存されている場合、ユーザーの操作により手動で同期を実行。
    - 同期処理では、ローカルとDrive間の差分を検出し、更新を行う（競合発生時の対応も考慮が必要）。
- Google Driveからの新規プロジェクト作成:
    - Google Drive内のフォルダを選択し、それを元にローカルに新しいプロジェクトを作成する。
    - この際、選択したDriveフォルダのアクセス情報（フォルダIDなど）をプロジェクトメタデータに保存し、以降の同期対象とする。
- ローカルプロジェクトのGoogle Driveへの初期連携:
    - ローカルで作成・編集しているプロジェクトを、ユーザーの操作によりGoogle Drive上の指定フォルダにアップロードし、連携情報をプロジェクトメタデータに保存する。
- 共有設定（Google Driveの機能を利用）。

---

## 3. UI/UXの方向性

### ツールバー/パレット
- 画面の端（左側や下部など）に、描画ツール（ペン、消しゴムなど）、選択ツール、カラーピッカーなどを配置。
- Adobe Frescoのように、選択中のツールに応じてオプションが変化するコンテキストメニュー。

### レイヤーパネル
- 画面の反対側の端に、階層的なリストを表示。
- プロジェクトのフォルダ構造をトップレベルのグループとして反映。
- 各フォルダグループ内には、サブフォルダグループや、そのフォルダ直下のGeoPackageファイルを表す「GeoPackageソース」アイテムが表示される。
- 「GeoPackageソース」アイテムは展開可能で、内部のレイヤーを表示・管理できる。
- ドラッグ＆ドロップでの並べ替え、スワイプでの表示/非表示など、直感的な操作。
- 画面の端に地図操作ツールバーを配置し、「てのひら」「ペン」「選択」などのツールを切り替え可能。

### ジェスチャー操作
- ピンチイン/アウトでのズーム。
- 2本指ドラッグでのパン。
- 長押しでのコンテキストメニュー表示など。

### ビジュアル
- シンプルでクリーンなデザイン。
- アイコンは分かりやすく、視覚的に魅力的なものを使用（フォルダ、GeoPackageファイル、各種レイヤータイプなど）。
- ダークモード対応。
- 地図操作ツールのアイコン（てのひら・ペン・選択など）を明示的に表示。
- ペン入力とタップ入力で挙動が異なることをUI上で分かりやすく表現。

---

## 4. 技術スタック案

- **フロントエンド**: Flutter (Dart)
    - クロスプラットフォーム対応 (iOS, Android)
    - 豊富なUIライブラリと描画API
- **状態管理**: Riverpod, Provider, BLoC/Cubitなど (Flutterでの標準的な選択肢)
- **GeoPackage操作ライブラリ (Dart)**:
    - `geopackage_dart`: (もし存在すれば。なければSQLiteライブラリとGeoPackage仕様に基づき自作または既存のものを探す)
    - `sqlite3` / `drift`: GeoPackageの実体はSQLiteデータベースなので、これらのライブラリで直接操作も可能。
- **地図表示**:
    - `flutter_map`: 高いカスタマイズ性を持つ地図ライブラリ。オフライン対応や多様なタイルソース利用に適している。
- **GPS**:
    - `geolocator`, `location` などのFlutterプラグイン。
- **Google Drive連携**:
    - `googleapis` パッケージ (Google Drive API v3)
    - `google_sign_in` プラグイン
- **バックエンド (オプション)**:
    - もし高度な共有機能やユーザー管理が必要な場合は、Firebase (Firestore, Authentication, Storage) などを検討。
- **地図操作ツール管理**: MapTool抽象クラス＋個別ツールクラス（PanTool, PenTool, SelectTool等）＋GlobalConfigで一元管理。
- **入力デバイス判別**: PointerEventのkindプロパティでペン/タップ/マウスを判別し、ツールごとに挙動を分岐。

### 4.1. フリーハンド描画の実装詳細

フリーハンドでの線・領域描画は、以下のFlutter技術要素を組み合わせて実装する。

- **ジェスチャー検出 (`GestureDetector`)**
    - `onPanStart`: 描画開始点を記録。
    - `onPanUpdate`: ドラッグ中の座標を連続的に記録。
    - `onPanEnd`: 描画終了処理、ジオメトリ確定。
- **描画処理 (`CustomPaint` と `CustomPainter`)**
    - 記録された座標リストに基づき、Canvasオブジェクトに線（Path）を描画する。
    - `onPanUpdate`の度に`setState`を呼び出し、リアルタイムに描画を更新する。
- **座標管理**
    - 描画中の座標は一時的なリスト（例: `List<Offset>`）に保持する。
    - 地図のズームレベルや表示範囲の変更に対応するため、画面座標と地理座標（緯度経度）の相互変換が必要 (`flutter_map`の機能を利用)。
- **線の平滑化 (オプション)**
    - より自然な曲線にするため、記録した座標点間にベジェ曲線やスプライン補間を適用することを検討。
    - Flutterの`Path`オブジェクトはベジェ曲線描画メソッド（`quadraticBezierTo`, `cubicTo`）を持つ。
- **GeoPackageへの保存**
    - 描画完了後、得られた地理座標のリストをLineStringまたはPolygonのジオメトリデータ（WKT: Well-Known Text や WKB: Well-Known Binary形式）に変換。
    - 変換したジオメトリデータを、属性情報と共にGeoPackageの該当レイヤーにフィーチャとして保存する。
- **領域の描画**
    - 線の描画と同様の仕組みで境界線をフリーハンドで描画。
    - 描画完了時にパスが閉じていなければ自動的に閉じるか、ユーザーに促す。
    - 閉じたパスからPolygonジオメトリを生成する。

---

## 5. 開発ステップ案 (MVPから段階的に)

### 1. MVP (Minimum Viable Product)
- ローカルでの新規プロジェクトフォルダ作成。
- プロジェクトフォルダのルートに配置された単一のGeoPackageファイルの読み込み・作成（点レイヤーのみ）。
- 地図上への点フィーチャ描画・属性（テキスト1つ）入力。
- GPSによる現在位置表示。
- `flutter_map` を用いた背景地図表示 (OpenStreetMapなど)。
- ローカルへのプロジェクト保存・読み込み。
- 基本的なレイヤー表示（GeoPackage内のレイヤーリスト）。

### 2. フェーズ2
- 線、領域フィーチャの描画機能追加（フリーハンド描画含む）。
- プロジェクトフォルダ内のサブフォルダを再帰的にスキャンし、複数のGeoPackageファイルを認識。
- フォルダ構造とGeoPackageファイルを反映した階層的なレイヤー/レイヤーグループ管理機能（表示、基本的な操作）。
- 基本的なGoogle Drive連携:
    - ローカルプロジェクトをGoogle Driveの新規フォルダへアップロードし、連携情報をメタデータに保存（初期連携設定）。
    - Google Drive上の既存プロジェクトフォルダをローカルへダウンロードして新規プロジェクトとして開始し、連携情報をメタデータに保存。
    - 手動での一方向同期（ローカル→Drive、Drive→ローカルのどちらかを選択、または最後に編集した方が優先などシンプルな競合解決）。

### 3. フェーズ3
- 高度な編集ツール（頂点編集、移動など）。
- 属性スキーマ対応、複数属性の編集。
- GPSトラッキング（軌跡記録）。
- プロジェクトメタデータ（`project_meta.json`）の読み書きの拡充。
- Google Drive同期機能の強化: 双方向同期、差分同期の検討、より高度な競合解決メカニズムの検討。

### 4. フェーズ4以降
- オフラインマップ対応 (`flutter_map`の機能を活用)。
- 計測ツール。
- UI/UXのさらなる洗練。
- Git連携の検討・実装。
- その他、ユーザーフィードバックに基づく改善。

---

## 6. レイヤジオメトリタイプ仕様（OGC Simple Features準拠）

K-MAPSで新規作成・選択できるレイヤのジオメトリタイプは、OGC（Open Geospatial Consortium）のSimple Features仕様（SFS）に準拠し、以下の3種類のみをサポートします。

### 対応ジオメトリタイプ

| タイプ名           | 説明                                                                                   | WKT例                                 |
|--------------------|----------------------------------------------------------------------------------------|---------------------------------------|
| MultiPoint         | 複数の点（Point）の集合。1つのレイヤ内で複数の点を管理。                               | `MULTIPOINT((10 40), (40 30), (20 20))` |
| MultiLineString    | 複数の線分（LineString）の集合。1つのレイヤ内で複数の線を管理。                        | `MULTILINESTRING((10 10, 20 20), (15 15, 30 15))` |
| MultiPolygon       | 複数のポリゴン（Polygon）の集合。1つのレイヤ内で複数のポリゴンを管理。                 | `MULTIPOLYGON(((30 20, 45 40, 10 40, 30 20)), ((15 5, 40 10, 10 20, 5 10, 15 5)))` |

#### 仕様詳細
- 単一のPoint, LineString, Polygonはサポートせず、必ずMulti*型（MultiPoint, MultiLineString, MultiPolygon）として扱う。
- 各レイヤは1種類のジオメトリタイプのみを持つ（混在不可）。
- 属性情報は各ジオメトリ（フィーチャ）ごとに付与可能。
- 仕様はQGISやOGC Simple Features Specification（ISO 19125-1）に準拠。

#### 参考リンク
- [QGIS公式ドキュメント: Geometry Handling](https://docs.qgis.org/latest/en/docs/pyqgis_developer_cookbook/geometry.html)
- [OGC Simple Feature Access Standard（外部）](https://www.ogc.org/standards/sfa) 